package algorithms

/*
	a histogram is a polygon composed of a sequence of rectangles
	aligned at common base line.

	given an array with heights of rectangles (assuming width is 1), we need
	to find the largest rectangle possible inside the histogram
*/

func LargestRectangleUnderHistogram(heights []int) int {
	// not able to solve yet :(
	// gonna go back to it soon

	return 0
}
